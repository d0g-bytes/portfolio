FROM node:20-slim

WORKDIR /workspace/frontend

# Copy package.json first
COPY package*.json ./

# Install dependencies as root (node_modules in volume)
RUN npm install

# Copy source code
COPY . .

# Change ownership of workspace
RUN chown -R node:node /workspace

USER node

EXPOSE 5173
CMD ["npm", "run", "dev"]
